#---*-perl-*-------------------------------------------------------
# uvm.hdl
#
#------------------------------------------------------------------
$hdl_spec = {
    -vlog_files   => ["src/uvm_pkg.sv"],
    -vlog_incdirs => ["src"],
    -vlog_opts    => ["-sverilog", "-timescale=1ps/1ps", ],
};
